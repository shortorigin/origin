version = "v2"

[[services]]
service_name = "compliance-service"
approved_workflows = ["compliance_attestation", "control_testing", "policy_exception", "quant_strategy_promotion"]
owned_aggregates = ["control_attestation", "compliance_checkpoint", "compliance_pack", "best_execution_report"]

[[services]]
service_name = "risk-service"
approved_workflows = ["continuity_activation", "policy_exception"]
owned_aggregates = ["risk_record", "treatment_plan"]
