version = "v1"
primary_service = "security-service"
approved_workflows = ["access_review", "continuity_activation", "environment_change", "incident_response", "release_approval", "vendor_onboarding"]
owned_aggregates = ["access_decision", "security_incident"]
