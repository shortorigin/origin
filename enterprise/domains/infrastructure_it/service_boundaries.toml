version = "v1"
primary_service = "infrastructure-service"
approved_workflows = ["access_review", "disaster_recovery_test", "environment_change"]
owned_aggregates = ["environment_change", "access_grant"]
