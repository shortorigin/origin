# Fuzzing Placeholder

- Learning goals: wire structured fuzz targets into Rust crates that enforce explicit parser and runtime bounds.
- Intended lab type: `cargo-fuzz` harnesses against synthetic policy and runtime inputs.
- Required tooling: `cargo`, `cargo-fuzz`, `cargo-nextest`.
- Issue seed: `[Infra]: expand nightly fuzz automation for the security teaching crates`.
