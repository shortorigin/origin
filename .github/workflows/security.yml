name: Security

on:
  pull_request:
  merge_group:
  push:
    branches:
      - main
  schedule:
    - cron: "0 7 * * 1"

permissions:
  contents: read

jobs:
  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Dependency review
        uses: actions/dependency-review-action@v4

  cargo-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cargo-bins: |
            cargo-audit

      - name: Doctor security toolchain
        run: cargo xtask doctor --domain security --ci

      - name: Run cargo audit
        run: cargo xtask run security-audit

  codeql:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: rust
            build-mode: manual
          - language: javascript-typescript
            build-mode: none
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality
          build-mode: ${{ matrix.build-mode }}

      - name: Setup Rust
        if: matrix.language == 'rust'
        uses: ./.github/actions/setup-rust
        with:
          install-sccache: "false"

      - name: Setup Linux UI packages
        if: matrix.language == 'rust'
        uses: ./.github/actions/setup-linux-ui

      - name: Build Rust workspace for CodeQL
        if: matrix.language == 'rust'
        run: cargo check --workspace --all-targets --all-features

      - name: Analyze
        uses: github/codeql-action/analyze@v3
        with:
          category: /language:${{ matrix.language }}

  security-gate:
    needs:
      - dependency-review
      - cargo-audit
      - codeql
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Assert security jobs succeeded
        env:
          DEPENDENCY_REVIEW_RESULT: ${{ needs.dependency-review.result }}
          CARGO_AUDIT_RESULT: ${{ needs.cargo-audit.result }}
          CODEQL_RESULT: ${{ needs.codeql.result }}
        run: |
          for result in "${DEPENDENCY_REVIEW_RESULT}" "${CARGO_AUDIT_RESULT}" "${CODEQL_RESULT}"; do
            if [[ "${result}" != "success" && "${result}" != "skipped" ]]; then
              echo "Security prerequisite failed with result ${result}"
              exit 1
            fi
          done
